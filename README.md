User forms built with Next.js and hosted on Azure Static Web Apps

## Getting Started

Run the development server:

```bash
pnpm run dev
```

Run type checker and linter:

```bash
pnpm typecheck && pnpm lint
```
